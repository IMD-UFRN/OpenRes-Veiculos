<!DOCTYPE html>
<html ng-app="OpenResVeiculosApp">
<head>
  <title>OpenRes - Veículos - Gerenciador de Veículos do IMD</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="navbar-brand" href="#">
          <%= image_tag "logo.png", width: 120, class: "image-logo" %>
        </a>

        <ul class="nav navbar-nav">
          <li><%= link_to "Reservas de Veículos", vehicle_reservations_path%></li>
          <li><%= link_to "Veículos", vehicles_path%></li>
        </ul>
        <div class="container-fluid nav-collapse">
          <ul class="text-right">
            <% if user_signed_in? %>
                Usuário:
                <%= link_to current_user.name, profile_path(current_user) %>
                <strong>(<%= current_user.email %></strong>)<br>
                 <%= link_to "Sair", destroy_user_session_path, method: :delete %>

            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="container">


    <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
        <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
          <a class="close" data-dismiss="alert">&#215;</a>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        </div>
      <% end %>
    <% end %>

  <%= yield %>

  </div>

  <footer>
   <!--  <p>&copy; <a href="https://github.com/IgorMarques">Igor Marques</a> e <a href="https://github.com/lucasbibiano">Lucas Bibiano</a></p> -->
  </footer>
</body>
</html>
